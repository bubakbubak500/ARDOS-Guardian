# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv-build/
.python311/
venv/

# PyInstaller
build/
dist/
*.spec.bak

# Guardian local state (per-station, not committed)
config.json
routes.json
*.log
logs/

# Local Claude Code settings (personal, not shared)
.claude/settings.local.json

# Generated icon (built on demand)
guardian/assets/guardian.ico

# Scratch files
_commitmsg.txt
_smoke*.py
_icon_preview.png
.ui-baseline-data/
.ui-baseline-data-frozen/
.downloads/
.build-temp/
artifacts/

# Locally built release artifacts (the small manifest remains versioned)
release/*.exe
release/*.zip
release/SHA256SUMS.txt
