# Normalise line endings: store text as LF in the repo.
* text=auto eol=lf

# Windows scripts keep CRLF on checkout.
*.ps1 text eol=crlf

# Binary assets (none tracked yet, but be explicit).
*.ico binary
*.png binary
